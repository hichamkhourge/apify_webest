# Apify SDK + the automation's runtime dependencies.
apify ~= 2.0
# Pin pydantic: 2.11+ breaks crawlee (pulled in by apify) with
# "cannot specify both default and default_factory" at import time.
pydantic ~= 2.10.0
# Pin browserforge: 1.2.4 renamed download.DATA_FILES -> DATA_DIRS, which
# crawlee's browserforge workaround still calls. 1.2.3 is the only version that
# both imports cleanly and satisfies crawlee's >=1.2.3 requirement.
browserforge == 1.2.3
selenium ~= 4.27
undetected-chromedriver ~= 3.5
webdriver-manager ~= 4.0
2captcha-python ~= 1.5
python-dotenv ~= 1.0
requests ~= 2.32
# undetected-chromedriver imports distutils, removed from the Python 3.12 stdlib.
setuptools >= 70.0
